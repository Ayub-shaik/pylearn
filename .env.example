# Example env file (no secrets)
APP_NAME=PyLearn
ENV=development

# LLM routing
LLM_PROVIDER=browser   # browser | ollama
OLLAMA_BASE_URL=http://localhost:11434

# Optional analytics (local-only by default)
ANALYTICS_ENABLED=false

