import type { ReactElement } from 'react';

export function Settings(): ReactElement {
  return (
    <main>
      <h2>Settings</h2>
      <p>TODO: Configure providers, voice preferences, and offline options.</p>
    </main>
  );
}
